<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white font-mono font-bold">
    <div
      class="w-full min-h-screen flex flex-col items-center p-6 sm:p-10 gap-10"
    >
      <!-- NavBar -->
      <div
        class="w-full flex flex-col sm:flex-row justify-between items-center gap-4"
      >
        <h1 class="text-3xl sm:text-4xl text-center sm:text-left">
          Edit Your Post
        </h1>
        <a
          class="py-2 px-4 bg-orange-500 rounded-lg text-black hover:bg-orange-400 transition text-center"
          href="/logout"
        >
          Log Out
        </a>
      </div>

      <!-- Edit Post Section -->
      <form
        action="/updatepost/<%= post._id %>"
        method="post"
        class="w-full max-w-xl flex flex-col items-center gap-4 bg-zinc-800 p-6 sm:p-8 rounded-xl shadow-lg"
      >
        <h2 class="text-xl sm:text-2xl font-bold mb-2 text-white">Edit Post</h2>

        <textarea
          name="newcontent"
          placeholder="What's on your mind?"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-500 rounded-lg outline-none text-white resize-none"
          rows="4"
          required
        >
<%= post.content %></textarea
        >

        <input
          type="submit"
          value="Update Post"
          class="bg-yellow-500 hover:bg-orange-500 text-black font-bold px-5 py-2 rounded-lg w-full sm:w-auto cursor-pointer"
        />
      </form>
    </div>
    <script src="./javascripts/script.js"></script>
  </body>
</html>
